

-- Création des tables
CREATE TABLE VILLE (ID int NOT NULL AUTO_INCREMENT PRIMARY KEY, NOM varchar(255) NOT NULL, POPULATION int NOT NULL, ID_DEPT int NOT NULL, ID_REG int NOT NULL);
CREATE TABLE DEPARTEMENT (ID int NOT NULL AUTO_INCREMENT PRIMARY KEY, CODE varchar(255) NOT NULL);
CREATE TABLE REGION (ID int NOT NULL AUTO_INCREMENT PRIMARY KEY, NAME varchar(255) NOT NULL);



-- Ajout des contraintes type « clés étrangères)
ALTER TABLE VILLE add constraint FK_VILLE_DEPT FOREIGN KEY (ID_DEPT) REFERENCES DEPARTEMENT(ID) ;
ALTER TABLE VILLE add constraint FK_VILLE_REG FOREIGN KEY (ID_REG) REFERENCES REGION(ID) ;